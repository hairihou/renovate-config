{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":timezone(Asia/Tokyo)",
    "group:definitelyTyped",
    "helpers:disableTypesNodeMajor",
    "helpers:pinGitHubActionDigestsToSemver"
  ],
  "ignorePresets": [":dependencyDashboard", ":pinDevDependencies"],
  "draftPR": true,
  "lockFileMaintenance": {
    "schedule": ["before 4am on the first day of the month"]
  },
  "prConcurrentLimit": 24,
  "rangeStrategy": "bump",
  "packageRules": [
    {
      "groupName": "linters",
      "extends": ["packages:linters"],
      "matchPackageNames": ["globals", "oxfmt"]
    },
    {
      "matchManagers": ["mise"],
      "minimumReleaseAge": "1 day"
    },
    {
      "matchManagers": ["mise"],
      "matchDepNames": ["claude-code", "codex", "opencode"],
      "minimumReleaseAge": "0 days"
    },
    {
      "matchDepNames": ["pipx:git+https://github.com/NousResearch/hermes-agent.git"],
      "fetchChangeLogs": "off"
    }
  ]
}
